import React from "react";
import { Text, TouchableOpacity, View } from "react-native";

const Button = () => {
    return (
        <TouchableOpacity>
            
        </TouchableOpacity>
    )
}

export default Button;